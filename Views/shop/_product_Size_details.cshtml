@model IEnumerable<ViewModels.Commans.SizeViewModel>


@if (Model.Count() > 0)
{
    foreach (var data in Model)
    {        
        <span data-id="@data.SizeId" class="@(ViewBag.selected == data.SizeId ? "active" : "")">@data.Size</span>
    }
}

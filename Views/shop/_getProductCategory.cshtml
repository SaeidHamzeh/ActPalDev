@model IEnumerable<ViewModels.Commans.CategoryViewModel>

@if (Model.Count() > 0)
{
    foreach (var data in Model)
    {
        <li><a href="/shop/category/@data.Category.Replace("&","and").Replace(" ","_")?catId=@data.CategoryId&ctype=cat">@data.Category</a></li>
    }
}
